import { SelectDateDirective } from './select-date.directive';

describe('AverageScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectDateDirective();
    expect(directive).toBeTruthy();
  });
});
