export type ButtonWidget = {
  label?: string;
  description?: string;
  add_label?: string;
  link: string;
  is_active: boolean;
  translateKey: string;
};

export type DashboardWidget = ButtonWidget;
