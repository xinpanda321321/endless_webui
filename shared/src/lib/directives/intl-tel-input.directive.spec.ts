import { IntlTelInputDirective } from './intl-tel-input.directive';

describe('IntlTelInputDirective', () => {
  it('should create an instance', () => {
    const directive = new IntlTelInputDirective();
    expect(directive).toBeTruthy();
  });
});
