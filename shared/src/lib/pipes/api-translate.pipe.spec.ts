import { ApiTranslatePipe } from './api-translate.pipe';

describe('ApiTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ApiTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
