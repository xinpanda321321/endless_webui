import { WorkflowStatePipe } from './workflow-state.pipe';

describe('WorkflowStatePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkflowStatePipe();
    expect(pipe).toBeTruthy();
  });
});
