import { AverageScorePipe } from './average-score.pipe';

describe('AverageScorePipe', () => {
  it('create an instance', () => {
    const pipe = new AverageScorePipe();
    expect(pipe).toBeTruthy();
  });
});
